import 'package:character_viewer/pages/character_list_view/character_list_view.dart';
import 'package:test/test.dart';

void main() {
  test('Counter value should be incremented', () {
    final listView = CharacterListView();

    // counter.increment();

    // expect(counter.value, 1);
  });
}